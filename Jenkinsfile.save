pipeline {
    agent {
	docker {
	    image 'node:24-alpine'
	}
    } 

    stages {
        stage('Install Dependencies') {
            steps {
                dir('backend') {
                    sh 'npm install'
                }
            }
        }
    }
}
