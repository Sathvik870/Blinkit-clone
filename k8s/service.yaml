apiVersion: v1
kind: Service

metadata:
  name: blinkit-backend-service

spec:
  selector:
    app: blinkit-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort
